/// @description 

cooldown = 0;

rotation = 45;//applied to image_angle

breakDistance = 32;//if object is more than 32 pixels away from player, it cannot be broken.

//Selector variables
selector_inst = noone;
selector_frame = 0;
selector_frame_number = sprite_get_number(spr_selector_b);